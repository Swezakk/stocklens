"""Страница «Портфель» дашборда (DESIGN.md §10.5).

Полная реализация (таблица позиций + P&L, форма позиции — единственный write-путь
дашборда, equity-кривая vs IMOEX, efficient frontier, риск-метрики) приходит
отдельным workflow. Здесь — минимальный заголовок-стаб для резолва навигации.
"""

import streamlit as st

#: Заголовок страницы (RU-копи — пользовательская строка).
_PAGE_TITLE = "Портфель"

#: Стаб-заметка до полной реализации страницы (DESIGN §10).
_IN_PROGRESS_NOTE = "Страница в разработке."


def render() -> None:
    """Отрисовать страницу «Портфель» (стаб до полной реализации)."""
    st.title(_PAGE_TITLE)
    st.info(_IN_PROGRESS_NOTE, icon=":material/construction:")
