"""StockLens dashboard — Streamlit-сервис аналитического дашборда.

Сервис говорит с API только по HTTP (инвариант спеки §3): прямого коннекта к БД
и импорта ORM-моделей нет. Из stocklens-core импортируются только доменные enum.
"""
